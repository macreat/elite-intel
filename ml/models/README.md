# Trained Model Artifacts

Storage location for serialized trained model binaries, encoders, and metric summaries.

**Contents:**
- Serialized scikit-learn models (`.joblib` or `.pkl` files) and model cards.

**Connects to:** Backend inference service endpoints.
