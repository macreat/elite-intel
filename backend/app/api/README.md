# API Routes

FastAPI router definitions mapping HTTP requests to service handlers.

**Contents:**
- `v1/` - Version 1 API routers (transactions, categories, products, dashboard, imports).
- `deps.py` - Dependency injection utilities (database sessions, auth).

**Connects to:** Services layer and Pydantic validation schemas.
