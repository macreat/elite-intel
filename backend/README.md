# Backend Service

FastAPI Python server providing RESTful endpoints, business logic validation, data access, and ETL import processing.

**Contents:**
- `app/` - Core application codebase (routes, services, models, database).
- `tests/` - Pytest unit and integration test suites.

**Depends on:** PostgreSQL database container and Python dependencies listed in `requirements.txt`.
