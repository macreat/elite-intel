# Database Specification

Relational database schemas, table relationships, and index strategy definitions.

**Contents:**
- `schema.md` - Transaction, Category, Product, and ImportBatch table field specs.
- `erd.png` - Entity-Relationship diagram visualization.

**Connects to:** SQLAlchemy models in `backend/app/models/` and PostgreSQL migrations.
