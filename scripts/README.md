# Utility & Maintenance Scripts

Automation scripts for data migration, database seeding, and CSV data extraction.

**Contents:**
- `import_data.py` - Command-line utility to parse legacy Excel/CSV files into PostgreSQL.
- `seed_database.py` - Script to populate initial categories and sample transaction data.

**Connects to:** Raw datasets in `data/raw/` and backend database repositories.
